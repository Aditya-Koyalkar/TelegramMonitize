"use client";

import { IGroup } from "@/@types/models";

type Props = {
  group: IGroup;
  anonymouskey: string;
};

const Checkout = ({ anonymouskey, group }: Props) => {
  return <></>;
};

export default Checkout;
